{
  "name": "molenaar-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server:install": "npm.cmd install --prefix server",
    "server:dev": "npm.cmd --prefix server run dev",
    "server:start": "npm.cmd --prefix server run start"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  }
}
